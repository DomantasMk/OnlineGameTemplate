export const TICK_RATE = 60;
export const TICK_MS = 1000 / TICK_RATE;

export const NETWORK_SEND_RATE = 20;
export const NETWORK_SEND_INTERVAL = Math.floor(TICK_RATE / NETWORK_SEND_RATE);

export const SERVER_PORT = 3001;

export const PLAYER_SPEED = 5.0;
export const PLAYER_SIZE = 1.0;

export const GRAVITY = -9.81;
export const GROUND_SIZE = 50;

export const INTERPOLATION_BUFFER_MS = 100;
